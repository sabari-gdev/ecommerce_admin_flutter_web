import 'package:flutter/material.dart';

class LoginPageWebView extends StatelessWidget {
  const LoginPageWebView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
